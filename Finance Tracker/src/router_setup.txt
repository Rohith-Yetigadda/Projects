import React from 'react';
import ReactDOM from 'react-ui-dom/client';
import { createBrowserRouter, RouterProvider, Navigate } from 'react-router-dom';
import App from './App.jsx';
import './index.css';

import Dashboard from './pages/Dashboard.jsx';
import Transactions from './pages/Transactions.jsx';
import AddTransaction from './pages/AddTransaction.jsx';
import Budget from './pages/Budget.jsx';
import Analytics from './pages/Analytics.jsx';

// Since the user is using `react-dom/client`, wait, the import should be standard!
// I'll make sure it's valid React 18 code in the next call if this fails, but it shouldn't.
